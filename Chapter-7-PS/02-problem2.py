l = ["Harry", "Ron", "Hermione", "Neville", "Luna"]

for name in l:
    if(name.startswith("H")):
        print(f"Hello {name}")
# In this code snippet, we have a list l containing several strings. We use a for loop to iterate over each element in the list.
# The variable i takes on the value of each element in the list l during each iteration of the loop.
# Inside the loop, we check if the current element (name) starts with the letter "H". If it does, we print the name.
